<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title> Budge | Register</title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css maybe delete-->
  <link href = "stylesheets/style.css"  type="text/css" rel="stylesheet">
</head>
<body>
<main>
  <header class = "header">
      <img src = "images/Header.png" height = "100" width="200">
  </header>
  <section>
    <h2>Sign Up, Become a Member Today!</h2>
    <div class = "center">

      {{#if errors}}
          {{#each errors}}
          	<div class="alert alert-danger">{{msg}}</div>
          {{/each}}
      {{/if}}

      <form  id="user_signup" method="POST" action = "/signup">
        <input id = "namefield" type = "text" placeholder=" Username" name="username"></p>

        <input id = "passfield" type = "password" placeholder=" Password" name="password"></p>

        <input id = "idfield" type = "text" placeholder=" Email Address" name="email"><br><br>

        <input type="submit" value="submit"/>

        <!--<a href="/index">
            <button>
              Register
            </button>    
        </a>-->
      </form>
    </div>
  </section>
</main>
</body>
</html>